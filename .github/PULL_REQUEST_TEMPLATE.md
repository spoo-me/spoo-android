## What this changes

<!-- One or two sentences. Link the issue if there is one: Closes #123 -->

## Why

<!-- The constraint or problem behind it. Skip if it is obvious from the title. -->

## Screenshots

<!-- Required for anything visual. Both light and dark theme if the change touches color. Delete this section otherwise. -->

## Checks

- [ ] `./gradlew lintDebug` passes
- [ ] `./gradlew testDebugUnitTest` passes
- [ ] `pre-commit run --all-files` passes
- [ ] Checked in both light and dark theme (if visual)

## Notes

- [ ] The commit type is right, because commit types on `main` decide the next release version
- [ ] Needs a backend or SDK change to land first
- [ ] Touches widget code, and the state it renders comes from Glance state rather than captured values
- [ ] Changes how the app is built or configured, and the README was updated
