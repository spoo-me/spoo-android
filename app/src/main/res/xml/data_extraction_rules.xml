<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ honors these over allowBackup: keep every DataStore
     (auth tokens above all) out of cloud backups AND device-to-device
     transfers, which can otherwise ignore allowBackup="false". -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="file" path="datastore/" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="datastore/" />
    </device-transfer>
</data-extraction-rules>
