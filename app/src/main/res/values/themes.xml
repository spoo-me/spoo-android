<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window chrome only; real theming happens in Compose. The window
         background must match the Compose surface or it flashes through
         transitions (and the launch frame) in the wrong tone. -->
    <style name="Theme.Spoo" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
